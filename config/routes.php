<?php
// setup the routes
//$router->routeAdd(uri,controller,view);
// if view is left blank it defaults to the same name as the controller

// this has to be the last entry as kind of a catch-all
$router->routeAdd("/","index");

?>
