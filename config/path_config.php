<?php

define('DEBUG',true);
define('APP_DIR',"");	// full path to your web app
define('BREMA_DIR',"");	// full path to the bremaframework
define('BASE_URI',"");	// base URI of your web app (e.g. if the address is http://localhost/web_app  'web_app' would be the base_uri)
define('SITE_URL',"");	// Site URL - this is mostly for use in your web app
define('LOGIN_URL',"");	// URL to redirect to when an unauthenticated user try to access an are that needs authentication

#define('ROUTE_FILE',''); 	// optional full path to the file where you have setup your routes if not using the default APP_DIR/config/routes.php
#define('VIEW_DIR','');		// optional full path to your view folder if not using the default APP_DIR/views

?>
